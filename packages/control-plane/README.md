# Control plane (R1-D)

Category folder for identity and control-plane packages. Organizes the repository only — not a dependency or ownership unit. Do not publish `@afenda/control-plane`.

| Package | Published name |
|---------|----------------|
| [`auth`](./auth/README.md) | `@afenda/auth` |
| [`admin`](./admin/README.md) | `@afenda/admin` |

Import by published name only (`@afenda/<name>`). Catalog: [packages/README.md](../README.md).
