# ARCH-022 / ARCH-028 S1.1 — pnpm workspaces (apps/web + packages/*).
packages:
  - "apps/*"
  - "packages/*"

settings:
  auto-install-peers: true
  strict-peer-dependencies: false
  prefer-frozen-lockfile: true
  manage-package-manager-versions: true
