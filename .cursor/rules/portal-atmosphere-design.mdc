---
description: Portal atmosphere hero design — references, assets, forbidden shortcuts (dormant fixture tree)
globs: public/brand/**
alwaysApply: false
---

# Portal atmosphere design

**Status:** Fixture tree under `components/portal-atmosphere/` was **hard-deleted** (2026-07-11). Do not remount it. Production `/auth/*` uses Studio login-page-02 + Neon. This rule applies to brand hero assets under `public/brand/**` only until an explicit reopen restores an experiment surface.

## Visual authority

| Theme | Reference |
|-------|-----------|
| Dark | `public/brand/heroes/auth-hero-dark.png` |
| Light | `public/brand/heroes/auth-hero-light.png` |
| Dual comp | `public/brand/heroes/auth-hero-dual.png` |

**Done = matches PNG at 1024px side-by-side.** See [`docs/architecture/slices/portal-atmosphere/pa-hero-quality-benchmark.md`](docs/architecture/slices/portal-atmosphere/pa-hero-quality-benchmark.md).

Layout zones: **editorial left** · **owl + deco center-back** · **access vault right**. Laptop target: balanced 3-column, `100svh`, no scroll.

## Assets (production)

| Role | Path |
|------|------|
| Dark owl (prod sharp preset) | `public/owl-variants/allowed-base/darkbg-removebg-preview2.png` |
| Light owl (prod sharp preset) | `public/owl-variants/allowed-base/whitebg-removebg-preview2.png` |
| **Comp-laptop base units** | same `allowed-base/` removebg PNGs + CSS deco |
| Variants (experiments) | `public/owl-variants/` |
| AI mockups (not prod) | `public/ref/` |

Use **separate dark/light owl assets**. Never fake light theme by inverting the dark owl.

## Forbidden without explicit user approval

- CSS `filter: invert()` / hue-rotate hacks on owl images for theme switching
- `preset="single"` or one PNG pasted as the hero (black-box sticker on page)
- Thin CSS circles as stand-in for celestial geometry / marble atmosphere from comps
- Wiring new hero experiments into production `/auth/**`
- Reintroducing anything listed in `pa-rejected-approaches.md`
- Recreating `components/portal-atmosphere/` without an explicit reopen

## Experiment path (dormant)

No on-disk fixture tree. Guardian / PA experiments stay closed until reopen. Local review of brand assets: `/playground` embed only.

Storybook has been removed from this repo — do not recreate `.storybook/` or `stories/`.
