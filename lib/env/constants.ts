/** Minimum length for Neon Auth session cookie signing secret (matches lib/auth/env.ts). */
export const MIN_NEON_AUTH_COOKIE_SECRET_LENGTH = 32;

/** Minimum length for seeded / preview client default password. */
export const MIN_CLIENT_DEFAULT_PASSWORD_LENGTH = 8;
